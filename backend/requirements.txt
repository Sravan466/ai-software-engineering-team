# ── Web framework ──
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20

# ── Database ──
SQLAlchemy==2.0.36
# Postgres driver (optional; SQLite needs nothing extra)
psycopg[binary]==3.2.3

# ── Orchestration ──
langgraph==0.2.60
langgraph-checkpoint-sqlite==2.0.1

# ── LLM clients ──
httpx==0.28.1            # Ollama + generic HTTP
anthropic==0.42.0        # Claude
openai==1.59.6           # GPT
google-generativeai==0.8.3   # Gemini

# ── Vector store / RAG ──
chromadb==0.5.23
pypdf==5.1.0

# ── Utilities ──
tenacity==9.0.0          # retry/backoff for provider calls
